public class Hp_R_T {
    public static void main(String[] args) {
        // hypotanuse of right triangle = underroot(a square + b square)
        double p = 5;
        double b = 12;
        double h = Math.sqrt(Math.pow(p, 2) + Math.pow(b, 2));
        System.out.println(h);
    }
}
